# HARD-TODO.md — Statuz 研究困难清单

> **最后更新：** 2026-06-19  
> **状态：** Post-66 Reboot — 旧 HARD-TODO 已归档至 leftover/

---

## 🔥 P0 — 必须先攻克

### 1. 图的工作记忆（Graph Working Memory）

**难度：** ⭐⭐⭐⭐

**问题本质：**
现在的 Engine 是被动查询的。但设计哲学要求**信息主动释放**——像人脑工作记忆，不需要"查询"就知道自己在哪、在干嘛。核心问题是：图中 1000+ 节点时，Agent 醒来该看哪 10 个？

**为什么是难题：**
1. **Attention budget** — Agent 上下文有限，不能把整张图塞进去
2. **Relevance decay** — 昨天的关键节点今天可能已经不重要了
3. **Change significance** — 不是每个 edge 增删都值得主动推送，但怎么判断？
4. **没有先例** — 现有系统（RAG、向量检索）都是被动检索，不是主动释放

**子问题：**
- 用什么指标衡量"Agent 当前最需要知道的信息"？（centrality + recency + task context）
- 变化的重要性怎么量化？（图差分 × 节点重要性 × Agent 任务相关性）
- 如何验证？——需要真实的 Agent 工作场景做 A/B 测试

**当前状态：** ❌ 未开始。只有 `Engine.impact()` 这样的被动查询。

**预估时间：** 3-6 个月研究 + 1-2 个月原型

---

### 2. 图 Loop 的数学定义

**难度：** ⭐⭐⭐⭐

**问题本质：**
"Better Loop" 是设计哲学的核心，但 Loop 到底是什么？STATUS → DETECT → UPDATE → STATUS 需要数学形式——输入什么、输出什么、收敛条件是什么。

**为什么是难题：**
1. **收敛条件** — Loop 怎么知道"够了"？不是靠命令，是靠图稳定性
2. **分层频率** — 代码变化秒级，人类决策天级，同一张图不同区域需要不同频率
3. **Event → Graph mapping** — 外部事件（git commit、Agent 操作）如何映射为图变化？规则是什么？

**子问题：**
- `diff(snapshot_t, snapshot_t+1)` 的阈值怎么定？
- 不同 node type 的 loop 频率如何分级？
- Loop 的中断和恢复机制是什么？

**当前状态：** ❌ 未定义。Engine 有 `diff()` 但没有 loop 机制。

**预估时间：** 2-4 个月研究 + 1 个月原型

---

## 🔬 P1 — 需要深入研究

### 3. 图的语义压缩（Semantic Graph Compression）

**难度：** ⭐⭐⭐⭐⭐

**问题本质：**
13 个节点可以全展示。13,000 个节点必须能自我抽象。图需要能**折叠子图为一个节点**——Louvain community detection 或类似的社区发现算法，自动识别"这些节点是一个子系统"。

**为什么是难题：**
1. **社区检测算法** — Louvain/Leiden 在大规模图上的性能
2. **多层抽象** — 折叠后的节点本身也可以参与更高级的社区检测
3. **可逆性** — 折叠后要能展开回到原始图
4. **语义保全** — 折叠不能丢失关键的依赖关系

**当前状态：** ❌ 未开始。Engine 只有基础的 degree centrality，没有社区检测。

**预估时间：** 6-12 个月研究 + 3-6 个月原型（需要图论教授参与）

---

### 4. 图的原生存储格式

**难度：** ⭐⭐⭐⭐⭐

**问题本质：**
当前纯内存，关了全丢。传统图数据库太重（Neo4j、ArangoDB 需要独立服务器），文件系统太平（不能高效遍历）。需要一种**以 edge 为中心的轻量存储格式**——因为 Statuz 的核心哲学是"箭头定义系统"。

**为什么是难题：**
1. **Edge-first 存储** — 大部分图数据库以 node 为中心，但 Statuz 需要以 edge 为中心
2. **社区分块** — 不是存整张图，而是按社区分块（类似 Git packfiles）
3. **实时性** — WAL（write-ahead log）+ 快照，查询时用内存缓存
4. **跨平台** — 不能依赖任何数据库服务器

**当前状态：** ❌ 未开始。Engine.toJSON() 可以把图序列化为 JSON，但这是临时的。

**预估时间：** 12-18 个月（需要存储系统专家）

---

### 5. 从代码到图的自动构建

**难度：** ⭐⭐⭐

**问题本质：**
现在 Engine 的节点和边是手工编码的（`buildStatuzTopology()`）。现实场景中，图必须从真实代码自动构建——读 package.json 生成 dependency 节点，读 AST 生成 import 边，读 git log 生成变更历史。

**为什么是难题：**
1. **多语言** — 不是只有 TypeScript，Python/Go/Rust 的 import 语法各不相同
2. **隐式依赖** — Docker Compose、环境变量、配置文件中的依赖不是 AST 能捕获的
3. **噪声过滤** — node_modules 里 1000 个包不需要全部进图

**当前状态：** ❌ 旧的 `detect --auto` 是 stub 实现（已归档）。需要重写。

**预估时间：** 2-3 个月

---

## 🧪 P2 — 验证与部署

### 6. 科学实验框架

**问题：** 我们怎么知道 Engine 真的有用？
- 需要 A/B 测试：有 Statuz 的 Agent vs 没 Statuz 的 Agent，在相同任务上的表现差异
- 需要量化指标：上下文恢复时间、任务完成率、偏离率（Agent 做错方向的比例）
- 需要可重复的实验流程

### 7. 自举测试

**问题：** Statuz 必须能吃自己的狗粮。
- Engine 运行在 Statuz 项目自身上
- Scanner 自动发现 engine/src/ 的结构
- Dashboard 展示 Statuz 开发 Statuz 的过程
- 这是一个**反射测试**——如果 Statuz 连自己都理解不了，就理解不了任何复杂项目

---

## 📋 当前进度总结

| # | 题目 | 难度 | 状态 | 依赖 |
|---|------|------|------|------|
| 1 | 图的工作记忆 | ⭐⭐⭐⭐ | ❌ | Engine 基础就绪 |
| 2 | 图 Loop | ⭐⭐⭐⭐ | ❌ | Engine.diff() 就绪 |
| 3 | 语义压缩 | ⭐⭐⭐⭐⭐ | ❌ | 需要社区检测算法 |
| 4 | 原生存储 | ⭐⭐⭐⭐⭐ | ❌ | 需要存储专家 |
| 5 | 代码→图构建 | ⭐⭐⭐ | ❌ | Engine 基础就绪 |
| 6 | 实验框架 | ⭐⭐ | ❌ | 方向一验证时需要 |
| 7 | 自举测试 | ⭐⭐⭐ | ❌ | 方向五完成后 |

> **诚实评估：** 当前 Engine 和 Dashboard 是概念验证级（PoC）。从 PoC 到产品，差距在研究 1-7 全部完成。
