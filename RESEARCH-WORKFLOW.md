# RESEARCH-WORKFLOW.md — Statuz 科学研究工作流

> **目的：** 确保 Statuz 的研发不是"拍脑袋写代码"，而是遵循可验证的科学方法。
> **适用：** 每一个研究方向和每一次实验。
> **原则：** 没有假设不动手，没有验证不宣称。

---

## 🔴 当前工作流的问题

回顾过去几轮的实际操作：

```
哲学论述 → 讨论 → 直接建 Engine → 直接建 Dashboard → 讨论研究方向
```

这里缺少了关键环节：

| 缺失环节 | 后果 |
|---------|------|
| ❌ 没有写下可证伪的假设 | 不知道建到哪算"成功" |
| ❌ 没有定义验证标准 | Dashboard 跑起来就算完了，但"跑起来"≠"有用" |
| ❌ 没有实验日志 | 不知道为什么选了邻接表而不是矩阵，下一个人得重新猜 |
| ❌ 研发和研究混在一起 | 边建边想"要不要加这个功能"，没有轻重缓急 |
| ❌ 没有量化指标 | "Engine 能查图"是定性描述，不能用来说服别人（或自己） |

这不是批评已经做的工作——Engine 和 Dashboard 的快速原型是对的，**但接下来必须更严谨。**

---

## 🟢 新工作流：Hypothesis-First Loop

```
┌──────────────┐
│  1. 写假设    │  ← 可证伪的陈述，不是愿望
└──────┬───────┘
       ▼
┌──────────────┐
│  2. 定指标    │  ← 什么数字算"证明"？什么数字算"证伪"？
└──────┬───────┘
       ▼
┌──────────────┐
│  3. 最小实现  │  ← 只写刚好能验证假设的代码
└──────┬───────┘
       ▼
┌──────────────┐
│  4. 跑实验    │  ← 记录数据，不美化结果
└──────┬───────┘
       ▼
   ┌───┴───┐
   │ 验证？ │
   └───┬───┘
  YES  │   NO
   ▼        ▼
┌─────┐  ┌──────────┐
│5. 记录│  │5. 记录失败│  ← 失败也是成果，告诉下一个人"此路不通"
│  成功│  │  修正假设  │
└──┬──┘  └─────┬────┘
   │           │
   ▼           ▼
┌──────────────┐
│  6. 合并成果  │  ← 成功的实验 → 合并到 Engine
└──────────────┘
```

## 📐 假设的写法

**错误示范**（不可证伪的愿望）：
> "Statuz 应该让 Agent 更好"

**正确示范**（可证伪的假设）：
> "当 Agent 通过 Engine.resume() 获取 top-5 工作记忆节点后，在 5 分钟内恢复正确任务上下文的准确率 ≥ 80%，对比基准（无 Statuz，仅靠对话历史）的 ≤ 40%。"

三条硬规则：
1. **有 null hypothesis**——"Statuz 不比随机好"是可以验证的
2. **有量化阈值**——不是"更好"，是"从 40% 提升到 80%"
3. **有对比基准**——不和空气比，和现有方法比

---

## 🧪 实验模板

每个实验一张卡片，放在 `engine/experiments/YYYY-MM-DD-hypothesis-name.md`：

```markdown
# Experiment: [简短标题]

## Hypothesis（可证伪）
[一句话陈述，含量化指标和对比基准]

## Null Hypothesis
"无效果"的版本——如果实验推翻不了它，说明我们的方法没用。

## Success Criteria
- ✅ 通过阈值：[具体数字]
- ❌ 证伪条件：[具体数字]
- ⚠️ 不确定区间：[数字范围]

## Method
- 输入数据：[什么数据、多少量]
- 实验步骤：[1. 2. 3.]
- 对比基准：[用什么方法做对照]

## Result
- 数据：[原始数据]
- 结论：[通过 / 证伪 / 不确定]
- 日期：[YYYY-MM-DD]

## Artifacts
- 代码：[链接到具体 commit]
- 数据：[链接到原始数据文件]

## Next Step
[如果通过] 合并到 Engine，进入下一个假设。
[如果证伪] 修正假设，或放弃这个方向。
[如果不确定] 增大样本量，或细化指标。
```

---

## 📊 当前状态审计

对已经建好的 Engine 和 Dashboard，我们现在补一个"追溯假设"：

### Engine v0.1 追溯审计

**隐性假设**（做了但没写）：
> "TypeScript 邻接表能在 100 节点 / 1000 边的图上，以 <10ms 的延迟完成 traverse/impact/path 三个核心查询。"

**验证状态：** ⚠️ 未量化。self-test 只证明了"能跑"，没测试性能阈值、没测大规模图、没定义"足够快"是多快。

**需要补的实验：**
1. 性能基准测试：10/100/1000/10000 节点的查询延迟曲线
2. 正确性测试：impact() 和 path() 在刻意构造的复杂图上是否返回正确结果
3. 内存测试：不同规模图的内存占用

### Dashboard v0.1 追溯审计

**隐性假设：**
> "一个包含 SVG 图 + 健康面板 + 点击交互的 Web Dashboard，能让开发者快速理解项目拓扑结构。"

**验证状态：** ❌ 未验证。没有做过用户测试，不知道"快速理解"是否成立。

---

## 🗓️ 下一步：第一个正式实验

从 HARD-TODO 的 P0 方向一开始：

### Experiment 001: Working Memory Surface

**假设（草案）：**
> "当 Agent 从图中获取 top-5 工作记忆节点（按 centrality × recency 排序）时，Agent 能在 3 分钟内恢复对项目状态的准确理解，准确率 ≥ 80%。"

**需要先做什么：**
1. 定义"准确理解"的量化标准（什么算"理解正确"？）
2. 准备测试场景（至少 3 个不同复杂度的项目）
3. 实现 `Engine.workingMemory(agentId, limit)` 方法
4. 搭建对比基准（无 Statuz 的纯对话历史方法）

**建议：** 这个实验不需要完美——第一个实验的目标是**把流程跑通**，验证"假设→实验→结论"这套方法本身。

---

## 🔒 工作流红线

1. **没有假设卡片不动手写功能代码** — 研究性代码可以写，但称不上"功能"
2. **假设必须可证伪** — 不能有任何形式的"不管怎样都算成功"
3. **失败必须记录** — 失败的实验和成功的实验同等重要
4. **一次只验证一个假设** — 不要同时测"工作记忆是否有效"和"Dashboard 是否好看"
5. **指标必须在实验前定** — 不能在看到结果后调整阈值
