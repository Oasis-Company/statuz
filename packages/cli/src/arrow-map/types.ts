/**
 * CLI Arrow Map types - re-exported from SDK for consistency.
 * 
 * This file serves as a thin wrapper to maintain compatibility with existing CLI code
 * while ensuring all Arrow Map types are consistent with the SDK.
 */

export type {
  ArrowMap,
  StatuNode,
  Arrow,
  ArrowType,
  ArrowProperties,
  ArrowMapVersion,
  ArrowMapStatus,
  NodeStatus,
  Criticality,
  DiscoveryMethod,
  TypeProperties,
  ArrowTemporal,
  ArrowMetadata,
  Invariant,
  TemplateParam,
  MapExtend,
  MapStorage,
  MapMetadata,
  NodeProperties,
  NodeMetadata,
  DependencyProperties,
  InformationFlowProperties,
  ResponsibilityProperties,
  ValidationProperties,
  ResourceTransferProperties,
  InfluenceProperties,
  ConstraintProperties
} from '@statuz/sdk-ts';
