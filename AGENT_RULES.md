# Agent Rules for Statuz Project

> 这些规则适用于所有参与 Statuz 项目开发的 AI Agent。

---

## Rule 1: 最苛刻标准衡量计划

**触发条件**：任何涉及新功能/架构的讨论

**执行要求**：
- 每个计划必须回答：这是用户明确要求的，还是 Agent 自以为需要的？
- 每个计划必须回答：这是否属于功能蔓延？是否超出当前阶段定义？
- 每个计划必须回答：如果不做这个，项目还能继续吗？
- 每个计划必须回答：这个计划的验收标准是什么？如何证明完成？

**禁止行为**：
- 不准假设用户需要某个功能
- 不准在没有用户确认的情况下添加"改进"
- 不准在没有验证的情况下扩展架构
- 不准用"以后可能需要"作为理由

**判断标准**：
```
如果答案是：
- 用户明确要求 → 可以继续
- AGENTS.md/Roadmap.md 定义 → 可以继续
- Agent自己想的 → 必须先问用户
- 文档里写着"不做" → 禁止继续
```

---

## Rule 2: 每次对话结尾校准定位

**触发条件**：每次对话结束

**执行要求**：
每次对话结束时，Agent 必须输出以下格式的定位校准：

```
## 📍 定位校准

### 当前状态
- 已完成：[具体完成了什么]
- 进行中：[当前正在做什么]
- 待处理：[明确的下一步是什么]

### 愿景 vs 现实
- [指出当前实现与愿景之间的差距，用最刻薄的语言]

### 风险与阻塞
- [列出当前的风险点、阻塞点、不确定点]

### 决策点
- [列出需要用户决策的问题，不要替用户做决定]

### 下一步选项
- [列出 2-4 个可选的下一步方向，让用户选择]
```

**禁止行为**：
- 不准在对话结束时说"任务完成"而不校准
- 不准假设下一步是什么而不问用户
- 不准隐藏风险或差距
- 不准替用户做战略决策

---

## Rule 3: 遵守项目边界

**必须遵守**：
- `AGENTS.md` 中定义的 P0/P1/P2 优先级
- `AGENTS.md` 中定义的 "Things Agents Must Not Do"
- `ROADMAP.md` 中定义的阶段目标
- `SPEC.md` 中定义的协议边界

**禁止越界**：
- P0 阶段不准做 P1/P2 的功能
- 不准添加 MCP Server（P0 明确禁止）
- 不准添加 Web Dashboard（P0 明确禁止）
- 不准添加 Embeddings（P0 明确禁止）
- 不准改变 License（需要用户明确批准）

---

## Rule 4: 代码质量标准

**必须做到**：
- 每次修改代码前，先读取相关文件
- 每次修改后，运行构建和测试验证
- 不准留下未解决的 TypeScript 错误
- 不准留下失败的测试

**禁止行为**：
- 不准在没有测试的情况下声称功能完成
- 不准在构建失败的情况下提交代码
- 不准添加不必要的依赖
- 不准引入网络调用（P0 禁止）

---

## Rule 5: 文档诚实原则

**必须做到**：
- 文档描述的功能必须与代码实现一致
- 如果代码没实现，文档必须标注"未实现"或"计划中"
- 如果功能是半成品，文档必须说明限制

**禁止行为**：
- 不准在文档里写"支持某功能"而代码是空的
- 不准用"即将支持"掩盖"根本没开始"
- 不准美化差距

---

## 执行检查清单

每次对话开始时，Agent 应自检：

```
□ 我是否理解了用户的真实意图？
□ 我是否检查了 AGENTS.md 的边界？
□ 我是否用最苛刻标准审视了计划？
□ 我是否准备好在结尾校准定位？
```

每次对话结束时，Agent 必须输出定位校准。