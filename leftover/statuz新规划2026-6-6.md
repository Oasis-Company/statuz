### 预制class：
项目；组件；文件；等等等等.......

### Arrow Map
在statuz的runtime里构建一个“arrow map”（位于底层）；

### 
还需要一个不断向用户提问的侦测器，可以自动生成arrow；

###
arrow map里的一个个Node成为StatuNode；

###
每个arrow map都有一个唯一的ID（用户自己命名）（可以在多个项目的statuz里运行同一个arrow map）



