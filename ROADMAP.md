# Statuz 研发路线图（ROADMAP）

> 状态：**草案 v1**，待逐轮复审。含方向与诚实声明，不含技术细节。

---

## 0. 我们站在哪

Statuz 已经有一副**靠得住、测过、结构正确**的地基：一套能容纳"项目关系"的引擎骨架。

但它现在还不能被任何一个 agent 真正用起来。**我们造好了骨架，还没有让任何东西活过来。** 这不是贬低——是诚实。地基是地基，不是建筑。

---

## 1. 这篇路线图的形态与原则

- 按**视线方向**推进，不按技术模块罗列——因为真正的难点不是"写哪些函数"，而是"往哪个方向走是对的"。
- 每个里程碑，都以**外部可感知的东西**为完成标志（某个人/某个 agent 看到或体验到变化），而不是内部指标。
- 只承诺**方向**，不承诺**日期和实现细节**。
- 所有**未知的未知**单独登记在 §4，并诚实标注"我们可能做不成"。
- North Star 始终是罗盘：每一步必须能追到 **context-recovery cost（上下文恢复成本）**。引擎数字只是手段。

---

## 2. 端到端里程碑（视线方向）

### M2 — 让 Statuz 用起来（第一次被 agent 用到）
- 对一个真实项目，自动产生它的"状态地图"。
- 把这个状态**主动释放**进 agent 的视野，让新会话的 agent"醒来即知"自己在哪、该干什么。
- 完成标志：一个 agent 在无辅助下需要做 N 步才能进入状态，有了 Statuz 后明显更少——**North Star 第一次能"数出来"**。
- 诚实声明：这一路的真正未知不是"能不能建图"，而是"释放哪些状态、放多大，才真的帮到 agent"。这个答案只能靠真实实验拿，不提前写好。

### M3 — 让状态活下去（随项目一起演化）
- 状态不能建完就死。项目在变，状态要给 agent 提供"最近什么变了、现在什么重要"。
- 完成标志：一个进行中的项目，agent 多次会话后，状态地图仍然准确、仍然被使用。
- 诚实声明：目前**没有可靠机制保证图始终跟得上现实**。怎么判断"这次变化值得更新状态"，仍是待解问题，可能长期停留在经验规则。

### M4 — 让状态串起来（跨项目、跨工具的生态位）
- 到达 Statuz 在更大生态里的定位：成为给系统/其它工具供给"状态"的一层底座。
- 完成标志：Statuz 作为**一处可选的状态来源**，被放进一个更大的体系里调用（不是被人告来告去的文档）。
- 诚实声明：**跨项目对齐是重灾区**。不同项目的语义墙很可能导致它短期做不成；我们先做单项目局部，不急着摊开。

### M5 — 让他人盖上楼（底座开放）
- Statuz 自己只做核心与交互模式；dashboard、工作流、各行业应用由**其他人**盖在这层底座上。
- 完成标志：外界能在 Statuz 之上造出 Statuz 从没想过要造的东西。
- 诚实声明：**Statuz 自己不去盖这些楼。** 这是边界，不是承诺。何时能发生不由我们单独决定。

---

## 3. 诚实声明：我们可能完不成的事

> 这条在别的 roadmap 里往往被藏起来。这里写全。

1. **"agent 醒来看哪几个节点"——注意力排序。** 这是 Statuz 研究抱负里最硬的一块，没有任何先例。它**可能永远停留在经验规则的水平**，甚至证明"排序帮不上多大忙"。我们接受这个可能。
2. **"图 Loop"的数学定义。** 状态多久更新一次、如何收敛，目前只有直觉、没有形式。它可能始终是一个工程猜测，而非一条科学定律。
3. **"主动释放真的比被动检索好吗"。** 评测口径之争会长期存在。**可能出现 A/B 永远有争议、证明不干净的情况**——那我们会如实写"这一条存疑"，而不是硬凹。
4. **跨项目对齐。** 可能因为语义墙根本做不成。到时我们会承认"这条方向错了"，而不是硬撑。
5. **项目的根本自疑。** 我们自己在笔记里写过："或许最开始就把一切复杂化了。" Statuz 可能被证明是一个**过早复杂化的问题**。如果真如此，我们做对的事是尽早发现并坦白。

---

## 4. 未知的未知（正式登记）

| # | 领域 | 我们不知道什么 | 诚实评级 |
|---|------|----------------|----------|
| 1 | 注意力排序 | "醒来该看哪几个节点"没有任何先例可言 | 🔴 极可能长期做不成 |
| 2 | 状态演化 | 怎么判断"这次变化值得更新状态" | 🟠 可能只在经验规则层 |
| 3 | 评测 | 怎么让人信"主动释放更好" | 🟠 A/B 可能永远有争议 |
| 4 | 生态位跨越 | 跨项目语义能否对齐 | 🟠 可能根本做不成 |
| 5 | 定义本身 | 图 Loop 有没有数学形式 | 🔴 可能只是工程猜测 |
| 6 | 对外叙事 | "工作记忆/注意力"词被其它赛道污染 | 🟡 影响存续，非技术 |

---

## 5. 当下正要去做的（下一步）

- **第一条"最小可见闭环"**：让 Statuz 对一个真实项目建成状态地图，并把它释放进一个 agent 的新会话，让"醒来即知"第一次可见、可数。
- 这是 M2 的最前一步。在此之前，不追加任何新东西——**一次只让一件关于状态的事活过来。**

---

## 6. 决不会做的（边界纪律）

- 不造产品外壳、不造 dashboard——那是盖在底座上的楼，留给别人。
- 做数据库/知识库/思维导图工具——Statuz 不是。
- 只给 AI 提供**状态**，不做思考本身。
- 对外不提版本、路线图细节、性能数字——在 M2 之前，这些都不该被承诺。

---

*草案 v1. 每个里程碑、每条诚实声明，都等你逐条复审确定。*